{
  "atom": "task-decompose",
  "version": "1.0",
  "cases": [
    {
      "id": "pass-01",
      "label": "Broad multi-source extraction — fan_out true with multiple scopes",
      "input": {
        "task": "Extract every role/title/acronym across all NIST 800-53 families",
        "corpus_hint": "20 per-family JSON files under cross-mapping/nist-catalog/output/"
      },
      "expected_output": {
        "fan_out": true,
        "write_access": false,
        "mode": "read-fanout",
        "scopes": {"$minItems": 2},
        "stop_conditions": {"$exists": true},
        "human_review_required": true
      },
      "expect_pass": true,
      "note": "Many independent files (a valid escalation trigger) justifies fan-out; a read task stays read-only (write_access false)"
    },
    {
      "id": "fail-01",
      "label": "Underspecified task — returns error, not fabricated scopes",
      "input": {"task": "fix it"},
      "expected_output": {
        "error": {"$contains": "underspecified"},
        "human_review_required": true
      },
      "expect_pass": true,
      "note": "Too vague to partition — must ask for detail, never invent scopes"
    },
    {
      "id": "edge-01",
      "label": "Canonical-update task — mutate mode with depth_cap 1",
      "input": {
        "task": "Add 5 new AI feed_registry entries and re-pin their source versions",
        "corpus_hint": "canonical-sources/feed_registry.json"
      },
      "expected_output": {
        "fan_out": true,
        "write_access": true,
        "mode": "mutate",
        "stop_conditions": {"depth_cap": 1},
        "human_review_required": true
      },
      "expect_pass": true,
      "note": "An EXPLICIT edit task is the only path to write_access:true + mutate (worktree-isolated, human-reviewed, depth_cap 1)"
    },
    {
      "id": "solo-01",
      "label": "Small single-source lookup — solo default, one agent",
      "input": {
        "task": "What roles does the NIST CA-6 control reference?",
        "corpus_hint": "one control in NIST_800_53_CA.json"
      },
      "expected_output": {
        "fan_out": false,
        "agent_count": 1,
        "escalation_trigger": null,
        "write_access": false,
        "scopes": {"$maxItems": 1},
        "human_review_required": true
      },
      "expect_pass": true,
      "note": "A task one agent can finish stays solo AND read-only — no escalation trigger, no edit request"
    }
  ]
}
